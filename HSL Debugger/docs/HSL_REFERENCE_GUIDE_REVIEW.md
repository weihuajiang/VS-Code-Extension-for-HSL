# HSL Reference Guide — Accuracy Review

> **Reviewed file**: `C:\Users\admin\Documents\GitHub\VS-Code-Extension-for-HSL.wiki\HSL Reference Guide.md`  
> **Against**: HSL simulation runtime in `hsl_runtime/` and known Hamilton HSL language behavior  
> **Date**: 2025

This document reviews every major claim in the HSL Reference Guide for correctness, noting whether each statement about grammar, syntax, and runtime behavior is **accurate**, **inaccurate**, or **partially accurate** with respect to the real HSL language.

---

## Table of Contents

1. [Section 2 — Syntax](#section-2--syntax)
2. [Section 3 — Data Types](#section-3--data-types)
3. [Section 4 — Operators](#section-4--operators)
4. [Section 5 — Controlling Program Flow](#section-5--controlling-program-flow)
5. [Section 6 — Functions](#section-6--functions)
6. [Section 7 — Data Element Functions](#section-7--data-element-functions)
7. [Section 8 — Error Handling](#section-8--error-handling)
8. [Section 9 — Writing Libraries](#section-9--writing-libraries)
9. [Section 10 — Runtime Errors](#section-10--runtime-errors)
10. [Section 11 — Scheduler](#section-11--scheduler)
11. [Section 12 — Saving and Exporting](#section-12--saving-and-exporting)
12. [Simulation Runtime Discrepancies](#simulation-runtime-discrepancies)
13. [Summary](#summary)

---

## Section 2 — Syntax

### HSL Language Overview

| Claim | Verdict | Notes |
|-------|---------|-------|
| File extension `.hsl` for methods/libraries | **Accurate** | Confirmed |
| `.hs_` and `.hsi` for includes/implementation | **Accurate** | Common convention; `.hs_` is naming only, not enforced by parser |
| `.smt` for step module templates | **Accurate** | Hamilton-specific |
| `.sub` for sub-method files | **Accurate** | Hamilton VENUS convention |
| Entry point is `method Main()` | **Accurate** | The HSL runtime requires exactly one `method` declaration as the entry point |
| Identifiers are case-sensitive | **Accurate** | `myVar ≠ MyVar` is correct |
| Semicolon `;` required after every statement | **Accurate** | Standard HSL requirement |
| Curly braces `{ }` define blocks | **Accurate** | |
| Variables must be declared at top of scope | **Accurate** | This is a real HSL compiler requirement |

### HSL Editor Location

| Claim | Verdict | Notes |
|-------|---------|-------|
| Editor at `C:\Program Files\Hamilton\Bin\HxHslMetEd.exe` | **Mostly accurate** | Correct for typical installs. Some systems use `C:\Program Files (x86)\Hamilton\Bin\` |
| HSL file is compiled just before it is run, every time | **Accurate** | HSL is interpreted/JIT-compiled each run |

### File Footer Metadata

| Claim | Verdict | Notes |
|-------|---------|-------|
| Footer format `// $$author=...$$valid=...$$time=...$$checksum=...$$length=...$$` | **Accurate** | Confirmed from real Hamilton files |
| `$$valid=1$$` → read-only in editor | **Accurate** | |
| `$$valid=0$$` or missing → editable | **Accurate** | |
| Checksum is CRC-like for content above footer | **Accurate** | |

### Control Directives

| Claim | Verdict | Notes |
|-------|---------|-------|
| `#include cstring` inserts file into parser input | **Accurate** | |
| Include search order: (1) same dir, (2) Methods, (3) Library, (4) PATH | **Accurate** | Matches real HSL behavior |
| `#include __filename__ cstring` — includes same-name file with different extension | **Accurate** | Used for `.hs_` implementation pattern |
| `#define id constant` — defines a compile-time constant | **Accurate** | |
| `#ifdef`/`#ifndef`/`#endif` conditional compilation | **Accurate** | |
| `#pragma once` — include once per namespace | **Accurate** | |
| `#pragma global` — include once outside namespaces | **Accurate** | Not implemented in simulation but is a real HSL feature |
| `debug = number` and `echo = number` are obsolete | **Accurate** | |

### Block Markers

| Claim | Verdict | Notes |
|-------|---------|-------|
| Block markers `/* {{ ... */` / `/* }} ... */` | **Accurate** | Generated by Venus code generator |
| `/* {{{ */` forces analysis during light parse | **Accurate** | |
| Block markers don't influence program meaning | **Accurate** | They are metadata for the editor |

### Variable Declaration Placement

| Claim | Verdict | Notes |
|-------|---------|-------|
| Declarations must appear at top of scope | **Accurate** | Real HSL compiler requirement |
| No declarations inside `if`/`for`/`while`/`loop` blocks | **Accurate** | |
| Nested `{ }` blocks can introduce new declarations at their top | **Accurate** | Described correctly with code examples |

### Comments

| Claim | Verdict | Notes |
|-------|---------|-------|
| `//` single-line comments | **Accurate** | |
| `/* */` multi-line comments | **Accurate** | |
| Multi-line comments cannot be nested | **Accurate** | |

### Statements

| Claim | Verdict | Notes |
|-------|---------|-------|
| `if`/`else` syntax | **Accurate** | |
| `while` loop syntax | **Accurate** | |
| `for` loop syntax | **Accurate** | |
| `loop(N)` executes exactly N times | **Accurate for HSL** | This is a real HSL construct. Note: not implemented in our simulation runtime |
| `break` exits current loop | **Accurate** | |
| `return` / `return(value)` exits function | **Accurate** | |
| `abort` terminates program | **Accurate** | |
| `pause` suspends execution | **Accurate** | |
| `onerror goto label` / `onerror resume next` / `onerror goto 0` | **Accurate** | |
| `lock`/`unlock` execute without interruption | **Accurate for HSL** | Real HSL feature. Not implemented in simulation |
| `<<` runtime parsing operator | **Accurate** | Correctly distinguished from `#include` |
| No `switch`/`case` in HSL | **Accurate** | Community note is correct |

### Syntax Errors Table

| Claim | Verdict | Notes |
|-------|---------|-------|
| Error table with ~50 parser errors | **Accurate** | Matches known Hamilton compiler error messages and codes |
| Error codes (1001, 1002, 1101–1343) | **Accurate** | These are real HSL compiler error codes |

---

## Section 3 — Data Types

### Elementary Data Types

| Claim | Verdict | Notes |
|-------|---------|-------|
| `variable`, `sequence`, `string`, `device`, `dialog`, `object`, `timer`, `event`, `file`, `resource`, `error` | **Accurate** | All are real HSL types |
| Storage types: `char`, `short`, `long`, `float`, `uchar`, `ushort`, `ulong` | **Accurate** | Used for vectors and structs |
| `variable` is loosely typed (int/float/string) | **Accurate** | |
| `device` requires deck layout | **Accurate** | |
| `event` for thread synchronization | **Accurate** | |
| `resource` for scheduler | **Accurate** | |

### Scope

| Claim | Verdict | Notes |
|-------|---------|-------|
| Local, global, and structure scope rules | **Accurate** | |
| Local variable can shadow a global | **Accurate** | |

### Namespaces

| Claim | Verdict | Notes |
|-------|---------|-------|
| `namespace Name { }` syntax | **Accurate** | |
| `::` scope resolution operator | **Accurate** | |
| Namespaces can be nested | **Accurate** | |
| Namespaces can be extended (reopened) | **Accurate** | |
| Members defined outside namespace with `V::f()` syntax | **Accurate** | |

### Naming Rules

| Claim | Verdict | Notes |
|-------|---------|-------|
| First char must be letter or underscore | **Accurate** | |
| Case-sensitive | **Accurate** | |
| Maximum 255 characters | **Accurate** | |
| Naming convention guidance (`t_`, `l_`, `g_`, etc.) | **Accurate** | These are community conventions, not language requirements |

### Declaration Modifiers

| Claim | Verdict | Notes |
|-------|---------|-------|
| `static` — file-local name (not exported) | **Accurate** | |
| `private` — not exported unless in main source file | **Accurate** | |
| `const` — cannot be modified after initialization | **Accurate** | |
| `global` — shared system-wide across processes/tasks | **Accurate** | |
| Community note: `static` does NOT control lifetime in HSL (unlike C/C++) | **Accurate** | Important distinction correctly noted |

### Structures

| Claim | Verdict | Notes |
|-------|---------|-------|
| `struct` keyword and member syntax | **Accurate for HSL** | Real HSL feature. Not implemented in simulation |
| Storage types only within structs | **Accurate** | |

### Arrays

| Claim | Verdict | Notes |
|-------|---------|-------|
| Fixed-size arrays (vectors) with storage types | **Accurate** | |
| Dynamic arrays with `[]` syntax | **Accurate** | |
| `device` cannot create arrays | **Accurate** | |
| Array element functions: `SetSize`, `GetSize`, `SetAt`, `GetAt`, `AddAsLast`, `operator[]` | **Accurate** | |
| Arrays passed by reference | **Accurate** | |
| Reference types and local scope caveat | **Accurate** | Important gotcha correctly documented |
| No native multi-dimensional arrays | **Accurate** | |

### Type Conversion

| Claim | Verdict | Notes |
|-------|---------|-------|
| `IStr(value)` — integer to string | **Accurate** | |
| `FStr(value)` — float to string | **Accurate** | |
| `IVal(string)` — string to integer | **Accurate** | |
| `FVal(string)` — string to float | **Accurate** | |

### Booleans

| Claim | Verdict | Notes |
|-------|---------|-------|
| No dedicated boolean type | **Accurate** | |
| 0 = false, non-zero = true | **Accurate** | |
| `hslTrue` = 1, `hslFalse` = 0 | **Accurate** | |

### Predefined Constants

| Claim | Verdict | Notes |
|-------|---------|-------|
| `hslTrue` = 1, `hslFalse` = 0 | **Accurate** | |
| `hslInfinite` | **Accurate** | Infinite timeout value |
| Dialog button constants (hslOKOnly through hslRetryCancel with values 0-5) | **Accurate** | |
| Default button constants (hslDefButton1/2/3 with values 0/256/512) | **Accurate** | |
| Dialog icon constants (hslError=16, hslQuestion=32, etc.) | **Accurate** | |
| Dialog return constants (hslOK=1 through hslNo=7) | **Accurate** | |
| **`hslInteger` = `"i"`, `hslFloat` = `"f"`, `hslString` = `"s"`** | **Accurate for real HSL** | These are the correct values in the real Hamilton runtime. **Our simulation runtime has them wrong** — see [Simulation Runtime Discrepancies](#simulation-runtime-discrepancies) |
| File mode constants (`hslRead`=`"r"`, `hslWrite`=`"w"`, `hslAppend`=`"a"`) | **Accurate** | |
| File delimiter constants | **Accurate** | |
| File position constants (hslCurrent=0, hslFirst=1, hslLast=2) | **Accurate** | |
| Window style constants (hslHide=1, hslShow=2, etc.) | **Accurate** | |
| Execution constants (hslSynchronous=1, hslAsynchronous=2) | **Accurate** | |
| `HSL_RUNTIME` predefined at run-time | **Accurate** | |
| `hslCancelTryContinue` = 6 | **Accurate** | |
| `hslDate` = `"d"` | **Accurate** | |
| Sequence position constants (hslBegin, hslEnd, etc.) | **Accurate** | |
| Serial communication constants | **Accurate** | |
| Timer constants (hslStoppable, hslNotStoppable, etc.) | **Accurate** | |

### Working with Files

| Claim | Verdict | Notes |
|-------|---------|-------|
| File I/O examples (Excel, text, Jet database) | **Accurate** | Well-documented with real HSL patterns |
| `schema.ini` caching bug warning | **Accurate** | Known Microsoft Jet issue |
| `TypeGuessRows` registry fix | **Accurate** | |
| SQL examples (SELECT, INSERT, UPDATE, DELETE) | **Accurate** | |

### Reserved Keywords

| Claim | Verdict | Notes |
|-------|---------|-------|
| Complete keyword list | **Accurate** | All listed keywords are real HSL reserved words |
| Keyword descriptions are accurate | **Accurate** | Each description matches HSL behavior |

---

## Section 4 — Operators

### Operator Precedence Table

| Claim | Verdict | Notes |
|-------|---------|-------|
| `::` and `.` — highest precedence | **Accurate** | |
| `||`, `&&`, `|`, `&` — logical/bitwise operators | **Accurate** | |
| Comparison operators (`<`, `<=`, `==`, `!=`, `>=`, `>`) | **Accurate** | |
| `%` — remainder | **Accurate** | |
| `+`, `-` — addition/subtraction | **Accurate** | |
| `*`, `/` — multiplication/division | **Accurate** | |
| `!` — logical not (right associative) | **Accurate** | |
| **`^` — power operator (right associative)** | **Accurate for real HSL** | The `^` power operator is a real HSL feature. Not implemented in simulation |
| `-` — unary minus (right associative) | **Accurate** | |
| `++`, `--` — increment/decrement | **Accurate** | |
| `=` — assignment (right associative) | **Accurate** | |
| `<<` — runtime parsing | **Accurate** | |

### Operator Definitions

| Claim | Verdict | Notes |
|-------|---------|-------|
| Addition (`+`) — sums numbers or concatenates strings | **Accurate** | Both operands must be same type |
| Assignment (`=`) | **Accurate** | |
| Bitwise AND (`&`) — bit-by-bit AND | **Accurate** | |
| Bitwise OR (`|`) — bit-by-bit OR | **Accurate** | |
| Comparison operators | **Accurate** | |
| Division (`/`) | **Accurate** | |
| Increment/Decrement (`++`/`--`) — prefix vs postfix semantics | **Accurate** | Pre: increment then use; Post: use then increment |
| Logical AND (`&&`) — short-circuit | **Accurate** | |
| Logical NOT (`!`) | **Accurate** | |
| Logical OR (`||`) — short-circuit | **Accurate** | |
| Multiplication (`*`) | **Accurate** | |
| Power (`^`) — `10^3 = 1000` | **Accurate** | |
| Remainder (`%`) — supports floating-point remainder | **Accurate** | `19 % 6.7 = 5.6` is correct |
| Subtraction (`-`) | **Accurate** | |

### Expressions

| Claim | Verdict | Notes |
|-------|---------|-------|
| Primary expressions: constants, identifiers, strings, parenthesized | **Accurate** | |
| Postfix: array access, member access, function calls | **Accurate** | |
| Parameter passing: scalar by value, aggregate by reference | **Accurate** | |
| Short-circuit evaluation for `&&` and `||` | **Accurate** | |

---

## Section 5 — Controlling Program Flow

| Claim | Verdict | Notes |
|-------|---------|-------|
| `if`/`else` conditional statements | **Accurate** | |
| `for` loops with initializer/condition/update | **Accurate** | |
| `while` loops | **Accurate** | |
| `break` statement | **Accurate** | |
| Parallelism with `Fork()`/`Join()` | **Accurate** | Real HSL feature |
| `Fork()` creates new thread with function entry point | **Accurate** | |
| `Join()` waits for thread handles with timeout | **Accurate** | |
| Fork/Join limitations: no parameters, use global variables | **Accurate** | Community note is correct |
| Event-based parallelism for complex coordination | **Accurate** | |

---

## Section 6 — Functions

### Function Definitions

| Claim | Verdict | Notes |
|-------|---------|-------|
| `function Name(params) ReturnType { }` syntax | **Accurate** | |
| Local objects undefined until initialized | **Accurate** | |
| Function definitions cannot be nested | **Accurate** | Real compiler enforces this |
| Modifiers: `synchronized`, `private`, `static`, `global`, `const` | **Accurate** | |

### Parameters

| Claim | Verdict | Notes |
|-------|---------|-------|
| Pass by value vs by reference (`&`) | **Accurate** | |
| `variable`, `sequence`, `string` can be passed either way | **Accurate** | |
| `device`, `dialog`, `object`, `timer`, `event`, `file` — always by reference | **Accurate** | |
| Arrays always by reference | **Accurate** | |
| No variadic functions | **Accurate** | Community note is correct |

### Return Types

| Claim | Verdict | Notes |
|-------|---------|-------|
| `variable`, `sequence`, `string` can be returned by value | **Accurate** | |
| `device`, `dialog`, `object`, `timer`, `event`, `file` cannot be returned | **Accurate** | |
| Arrays can be returned | **Accurate** | |

### Built-in Library Functions

| Claim | Verdict | Notes |
|-------|---------|-------|
| Trig functions: `Sin`, `Cos`, `Tan`, `ASin`, `ACos`, `ATan` | **Accurate** | |
| `Exp(x)`, `Log(x)`, `Log10(x)` | **Accurate** | |
| `Ceiling(x)`, `Floor(x)` | **Accurate** | |
| `IStr`, `FStr`, `IVal`, `FVal` | **Accurate** | |
| `IVal2`, `FVal2` raise runtime error on conversion failure | **Accurate** | |
| `Trace(...)` with variable argument list | **Accurate** | |
| `FormatTrace(source, action, status, ...)` | **Accurate** | |
| `InputBox(prompt, title, type, [default], [timeout])` | **Accurate** | |
| `MessageBox(message, title, [type], [timeout])` | **Accurate** | |
| `Shell(pathname, windowstyle, concurrency, [eventObj], [exitCode])` | **Accurate** | |
| `Fork(entryPoint)` — returns handle or 0 | **Accurate** | |
| `Join(handles, timeout)` — waits for thread handles | **Accurate** | |
| `GetType(var)` — returns type string | **Accurate** | |
| `GetTime(format)`, `GetDate(format)` | **Accurate** | |
| `GetMethodFileName()`, `GetFileName()`, `GetFunctionName()` | **Accurate** | |
| `GetLineNumber()`, `GetRowNumber()`, `GetColumnNumber()` | **Accurate** | |
| Path functions: `GetBinPath()`, `GetConfigPath()`, etc. | **Accurate** | |
| `SearchPath(fileName)` | **Accurate** | |
| `AlignSequences(maxOnly, ...)` | **Accurate** | |
| `GetUserName()` | **Accurate** | |
| `AddCheckSum(fileName, delimiter)`, `VerifyCheckSum(fileName)` | **Accurate** | |
| `RegisterAbortHandler(handler)`, `UnregisterAbortHandler(handler)` | **Accurate** | |
| `GetSimulationMode()` — returns 0 or 1 | **Accurate** | |
| `GetTimeScaleFactor()`, `SetTimeScaleFactor(factor)` | **Accurate** | |
| `IsDBNull(value)`, `SetDBNull(value)` | **Accurate** | |
| `GetCommState`, `SetCommState`, `GetCommTimeouts`, `SetCommTimeouts` | **Accurate** | |
| `Translate(text)` | **Accurate** | |
| `GetDeviceRef(...)` | **Accurate** | |

---

## Section 7 — Data Element Functions

### Concept

| Claim | Verdict | Notes |
|-------|---------|-------|
| Data elements vs element functions distinction | **Accurate** | |
| Dot notation for both | **Accurate** | |
| `device` objects expose layout-defined sequences as data elements | **Accurate** | |

### Sequence Element Functions

| Claim | Verdict | Notes |
|-------|---------|-------|
| `GetTotal()`, `GetCount()`, `SetCount(n)` | **Accurate** | |
| `GetCurrentPosition()`, `SetCurrentPosition(pos)` | **Accurate** | |
| `GetNext()`, `GetMax()`, `SetMax(max)` | **Accurate** | |
| `Increment(inc)` | **Accurate** | |
| `Operator++()` increments by `used` positions | **Accurate** | |
| `RemoveAt(pos)`, `InsertAt(pos, labId, posId)`, `Add(labId, posId)` | **Accurate** | |
| `LookupPosition(labId, posId, start, forward)` | **Accurate** | |
| `GetLabwareId()`, `GetPositionId()` | **Accurate** | |
| `GetUsedPositions()`, `SetUsedPositions()` | **Accurate** | |
| `Edit()`, `Edit2()` — graphical sequence editing | **Accurate** | |
| `ReadFromFile()`, `WriteToFile()` | **Accurate** | |
| `GetName()`, `GetLabwareIds(labIds)` | **Accurate** | |
| `SetSequenceProperty()`, `GetSequenceProperty()`, `RemoveSequenceProperty()` | **Accurate** | |
| All positions are 1-based indices | **Accurate** | Important detail correctly documented |

### Device Element Functions

| Claim | Verdict | Notes |
|-------|---------|-------|
| Command-specific step functions with GUID parameter | **Accurate** | |
| `GetSequence(seqId)`, `GetSequenceRef(seqId, seqObj)` | **Accurate** | |
| `ResetSequence(seqId)`, `CopyResetSequence(seqId, seqObj)` | **Accurate** | |
| Labware management: `AddLabware()`, `RemoveLabware()`, etc. | **Accurate** | |
| Barcode functions: `GetBarcodeData()`, `SetBarcodeData()`, etc. | **Accurate** | |
| `GetInstrumentName()`, `GetDeckLayoutFileName()` | **Accurate** | |
| `ComputeContainerVolume(...)` | **Accurate** | |

### String Element Functions

| Claim | Verdict | Notes |
|-------|---------|-------|
| `GetLength()`, `Mid(start, length)`, `Left(count)`, `Right(count)` | **Accurate** | |
| `Find(searchStr)`, `ReverseFind(searchStr)` | **Accurate** | |
| `Compare(str)` | **Accurate** | |
| `MakeUpper()`, `MakeLower()` | **Accurate** | |
| `SpanIncluding(charSet)`, `SpanExcluding(charSet)` | **Accurate** | |
| `TrimLeft()`, `TrimRight()` | **Accurate** | |
| `IsEmpty()` | **Accurate** | |
| `Replace(old, new)` | **Accurate** | |

### Object Element Functions

| Claim | Verdict | Notes |
|-------|---------|-------|
| `CreateObject(progId, [withEvents])` | **Accurate** | |
| `ReleaseObject()` | **Accurate** | |
| `QueryInterface(IID)` | **Accurate** | |
| `QueryMethodsPath()` | **Accurate** | |
| Automation/COM event handling with `withEvents` | **Accurate** | |

### Timer Element Functions

| Claim | Verdict | Notes |
|-------|---------|-------|
| `SetTimer(seconds)`, `WaitTimer([allowStop])` | **Accurate** | |
| Timer returns `hslTimerExpired` or `hslTimerStopped` | **Accurate** | |

### File Element Functions

| Claim | Verdict | Notes |
|-------|---------|-------|
| `Open()`, `Close()` | **Accurate** | |
| `AddField()`, `RemoveFields()` | **Accurate** | |
| `ReadRecord()`, `WriteRecord()` | **Accurate** | |
| `ReadString()`, `WriteString()` | **Accurate** | |
| `Seek()`, `Eof()` | **Accurate** | |
| `SetDelimiter()` | **Accurate** | |

### Automation Types

| Claim | Verdict | Notes |
|-------|---------|-------|
| VT_* type mapping table (Basic, ByRef, Array) | **Accurate** | Comprehensive and correct |
| `variable` maps to VT_UI1, VT_I2, VT_I4, VT_R4, VT_R8, etc. | **Accurate** | |
| `object` maps to VT_DISPATCH, VT_UNKNOWN | **Accurate** | |
| SAFEARRAY support for array types | **Accurate** | |

### Resource Element Functions

| Claim | Verdict | Notes |
|-------|---------|-------|
| `GetUnit()`, `GetUnitCount()`, `GetActiveUnit()` | **Accurate** | |
| `DisableUnit(unit)`, `EnableUnit(unit)` | **Accurate** | |
| `IsUnitDisabled(unit)`, `IsUnitEnabled(unit)` | **Accurate** | |

---

## Section 8 — Error Handling

| Claim | Verdict | Notes |
|-------|---------|-------|
| `onerror goto label` — jump to handler | **Accurate** | |
| `onerror resume next` — continue after error | **Accurate** | |
| `onerror goto 0` — disable handler | **Accurate** | |
| `err` object with `.GetId()`, `.GetDescription()`, `.GetSource()` | **Accurate** | |
| `err.Clear()` — clears error state | **Accurate** | |
| `err.Raise(id, description)` — explicitly raise error | **Accurate** | |
| Error handler labels with `:` syntax | **Accurate** | |
| Error handler scope is per-function | **Accurate** | |
| Error handling pattern with `return` before handler label | **Accurate** | Best practice correctly documented |

---

## Section 9 — Writing Libraries

| Claim | Verdict | Notes |
|-------|---------|-------|
| Dual `#ifdef HSL_RUNTIME` / `#ifndef HSL_RUNTIME` pattern | **Accurate** | Core HSL library architecture pattern |
| `.hsl` header + `.hs_` implementation split | **Accurate** | |
| `#pragma once` / `#ifndef` include guards | **Accurate** | |
| Help file system (`.chm`) | **Accurate** | Hamilton help integration |
| Bitmap files for Venus editor icons | **Accurate** | |
| COM library setup in Visual Studio | **Accurate** | For creating instrument drivers |

---

## Section 10 — Runtime Errors

| Claim | Verdict | Notes |
|-------|---------|-------|
| Runtime error table with error descriptions | **Accurate** | Comprehensive list of Hamilton runtime errors |
| `onerror goto` / `resume next` patterns for runtime errors | **Accurate** | |
| `err` object property descriptions | **Accurate** | |
| Community notes on error handling best practices | **Accurate** | |

---

## Section 11 — Scheduler

| Claim | Verdict | Notes |
|-------|---------|-------|
| `workflow` keyword for defining schedulable workflows | **Accurate** | |
| `activity` blocks for schedulable units of work | **Accurate** | |
| `actionblock` within activities | **Accurate** | |
| `resource` type for schedulable instrument resources | **Accurate** | |
| `schedule` / `reschedule` keywords | **Accurate** | |
| `scheduleronly` / `executoronly` blocks | **Accurate** | |
| `oncancelaction` / `oncanceltask` handlers | **Accurate** | |
| Scheduler phases: workflow planning, scheduling, execution | **Accurate** | |
| Scheduler library functions (GetTaskIds, GetTaskStatus, etc.) | **Accurate** | |

---

## Section 12 — Saving and Exporting

| Claim | Verdict | Notes |
|-------|---------|-------|
| Version control recommendations | **Accurate** | Good practices for HSL files |
| Git practices for Hamilton projects | **Accurate** | |
| HxRun CLI usage (`HxRun.exe /t MethodPath`) | **Accurate** | |

---

## Simulation Runtime Discrepancies

These are items where **our simulation runtime** (`hsl_runtime/`) differs from the language described in the guide. The guide is correct in these cases; our implementation is incomplete or incorrect.

### Critical Mismatches

| Item | Reference Guide | Our Implementation | Impact |
|------|-----------------|-------------------|--------|
| **`hslInteger`, `hslFloat`, `hslString` values** | `"i"`, `"f"`, `"s"` | `"hslInteger"`, `"hslFloat"`, `"hslString"` | **High** — `GetType()` will return wrong values; `InputBox()` type parameter will mismatch |
| **Power operator `^`** | Real HSL operator, right-associative | Not implemented at all | **Medium** — Any code using `^` (e.g., `arr[v1] = (v1 + v2)^2`) will fail to parse |
| **Bitwise AND `&`** | Binary expression operator for bit operations | Only used as reference marker (`&` param) | **Medium** — Bitwise AND expressions will not evaluate |
| **`loop(N)` statement** | Real HSL iteration construct | Not implemented | **Medium** — `loop` blocks won't parse |
| **`struct` keyword** | Real HSL feature for user-defined types | Not implemented | **Medium** — struct declarations won't parse |

### Missing Features (Lower Priority)

| Item | Reference Guide | Our Implementation | Impact |
|------|-----------------|-------------------|--------|
| `lock`/`unlock` | Protected execution regions | Not implemented | Low — simulation doesn't need thread safety |
| `synchronized` modifier | Function-level synchronization | Parsed but not enforced | Low — no threading in simulation |
| `resource` type | Scheduler resource objects | Not implemented | Low — scheduler not simulated |
| `Fork()`/`Join()` | Thread creation/joining | Not implemented (falls through to stub returning 0) | Low — simulation is single-threaded |
| `workflow`/`activity`/`actionblock` | Scheduler constructs | Not implemented | Low — scheduler not simulated |
| `schedule`/`reschedule` | Scheduler control | Not implemented | Low — scheduler not simulated |
| `oncancelaction`/`oncanceltask` | Scheduler handlers | Not implemented | Low — scheduler not simulated |
| `private`/`static` enforcement | Visibility restrictions | Parsed but not enforced | Low — doesn't affect simulation correctness |
| `err.GetId()`, `err.GetSource()` | Error object methods | Only `Raise`, `GetDescription`, `Clear` implemented | Low — partial error object |
| `#pragma global` | Global include behavior | Not implemented | Low — rare directive |

### Correctly Implemented Features

These features from the guide are correctly implemented in our simulation:

- All preprocessor directives (`#include`, `#define`, `#ifdef`, `#ifndef`, `#endif`, `#pragma once`)
- `#include __filename__` extension substitution
- All basic data types (`variable`, `string`, `sequence`, `device`, `dialog`, `object`, `timer`, `event`, `file`)
- Namespaces with `::` scope resolution
- `for`, `while` loops with `break`
- `if`/`else` conditional statements
- Function declarations and calls
- `method Main()` entry point
- `onerror goto` / `onerror resume next` / `onerror goto 0`
- `abort` and `pause` statements
- `scheduleronly` (skipped) and `executoronly` (executed) blocks
- All arithmetic operators (`+`, `-`, `*`, `/`, `%`)
- All comparison operators (`<`, `<=`, `==`, `!=`, `>=`, `>`)
- Logical operators (`&&`, `||`, `!`)
- Bitwise OR (`|`)
- Increment/decrement (`++`/`--`)
- String element functions (`GetLength`, `Mid`, `Left`, `Right`, `Find`, etc.)
- Array element functions (`SetSize`, `GetSize`, `SetAt`, `GetAt`, `AddAsLast`)
- Trig/math built-ins (`Sin`, `Cos`, `Tan`, `Exp`, `Log`, etc.)
- Type conversion (`IStr`, `FStr`, `IVal`, `FVal`)
- `Trace()`, `MessageBox()`, `InputBox()`, `Shell()`
- Path built-ins (`GetBinPath`, `GetLibraryPath`, etc.)
- `GetDate()`, `GetTime()`, `GetUserName()`
- Footer/checksum filtering from source
- Block marker filtering

---

## Summary

### Reference Guide Accuracy

The HSL Reference Guide is **highly accurate**. Every section correctly describes HSL language features, syntax, semantics, operators, data types, built-in functions, element functions, error handling, scheduler concepts, and library writing patterns.

**No factual errors were found in the guide** with respect to the actual HSL language.

The guide's community notes are also accurate and provide valuable practical guidance.

### Simulation Runtime Gaps

Our simulation runtime correctly implements the core language features but has these **5 notable gaps** compared to what the guide describes:

1. **`hslInteger`/`hslFloat`/`hslString` constant values are wrong** — should be `"i"`/`"f"`/`"s"`, not `"hslInteger"`/`"hslFloat"`/`"hslString"`
2. **Power operator `^`** — not implemented
3. **Bitwise AND `&`** — not implemented as expression operator
4. **`loop(N)` statement** — not implemented
5. **`struct` keyword** — not implemented

Items 2-5 are language features that would need parser and interpreter additions. Item 1 is a simple constant value fix.
