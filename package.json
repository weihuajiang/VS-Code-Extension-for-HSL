{
  "name": "hsl-language-support",
  "displayName": "HSL (Hamilton) Language Support",
  "description": "Base language support for Hamilton Standard Language (HSL)",
  "version": "1.9.9",
  "publisher": "ZacharyMilot",
  "author": "Zachary Milot",
  "repository": {
    "type": "git",
    "url": "https://github.com/zdmilot/VS-Code-Extension-for-HSL.git"
  },
  "bugs": {
    "url": "https://github.com/zdmilot/VS-Code-Extension-for-HSL/issues"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Programming Languages", "Linters", "Other"],
  "icon": "HSL.png",
  "main": "./out/extension.js",
  "activationEvents": ["onLanguage:hsl"],
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  },
  "contributes": {
    "languages": [
      {
        "id": "hsl",
        "aliases": ["HSL", "Hamilton Standard Language"],
        "extensions": [".hsl", ".hs_", ".sub"],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "hsl",
        "scopeName": "source.hsl",
        "path": "./syntaxes/hsl.tmLanguage.json"
      }
    ],
    "snippets": [
      {
        "language": "hsl",
        "path": "./snippets/hsl.code-snippets"
      }
    ],
    "customEditors": [
      {
        "viewType": "hsl.methodEditor",
        "displayName": "Hamilton HSL Method Editor",
        "selector": [
          {
            "filenamePattern": "*.hsl"
          },
          {
            "filenamePattern": "*.hs_"
          },
          {
            "filenamePattern": "*.sub"
          }
        ],
        "priority": "option"
      }
    ],
    "commands": [
      {
        "command": "hsl.runMethod",
        "title": "Run HSL Method",
        "category": "HSL",
        "icon": "$(play)"
      },
      {
        "command": "hsl.openInHamiltonEditor",
        "title": "Open in Hamilton HSL Editor",
        "category": "HSL"
      }
    ],
    "menus": {
      "editor/title/run": [
        {
          "command": "hsl.runMethod",
          "when": "resourceLangId == hsl",
          "group": "navigation"
        }
      ],
      "editor/context": [
        {
          "command": "hsl.runMethod",
          "when": "resourceLangId == hsl",
          "group": "navigation"
        },
        {
          "command": "hsl.openInHamiltonEditor",
          "when": "resourceLangId == hsl && hsl.hamiltonEditorAvailable",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "hsl.openInHamiltonEditor",
          "when": "resourceLangId == hsl && hsl.hamiltonEditorAvailable",
          "group": "navigation"
        }
      ]
    },
    "keybindings": [
      {
        "command": "hsl.runMethod",
        "key": "f5",
        "when": "resourceLangId == hsl"
      }
    ],
    "configurationDefaults": {
      "[hsl]": {
        "github.copilot.chat.codeGeneration.instructions": [
          {
            "text": "HSL (Hamilton Standard Language) is a C-like language for Hamilton liquid handling robots. It has critical differences from C/C++/JavaScript that MUST be followed:\n\n1. VARIABLE DECLARATIONS MUST BE AT THE TOP OF EVERY CODE BLOCK: All variable declarations must appear BEFORE any executable statement in the same { } block. This includes function bodies, method bodies, namespace bodies, struct bodies, nested blocks, and file scope. Declaring a variable after any executable line in the same block is a COMPILE ERROR. Nested { } blocks reset the rule -- declarations can appear at the top of nested blocks.\n\n2. 'continue' IS NOT A VALID KEYWORD: HSL does not support 'continue' in loops. Use if/else restructuring instead. For example, instead of 'if (cond) { continue; }' use 'if (!cond) { /* body */ }'.\n\n3. NO COMPOUND ASSIGNMENT OPERATORS: HSL does not have +=, -=, *=, /= operators. Use expanded form: 'x = x + 1;' instead of 'x += 1;'. Also '=+' and '=-' are not valid -- use '= +' or '= -' (with a space).\n\n4. FUNCTIONS REQUIRE BOTH A DECLARATION AND A DEFINITION: Every function needs a forward declaration (prototype) AND a definition (implementation). Missing either is an error. Duplicate definitions are also errors.\n\n5. DATA TYPES: variable (general-purpose), string, float, short, long, char, object, sequence, device, resource, dialog, timer, event, file.\n\n6. MODIFIERS: private, static, const, global, synchronized.\n\n7. FUNCTION SYNTAX: 'function Name(variable param) returnType { }' -- return type is required (use 'void' for no return). Methods use: 'method main() { }'.\n\n8. STRING CONCATENATION: Use '+' operator or StrConcat() function.\n\n9. ERROR HANDLING: Use 'onerror goto Label;' pattern with labeled error handler blocks. Use err.GetDescription(), err.Clear(), abort.\n\n10. CONTROL FLOW: if, else, for, while, do, loop, switch, case, default, break, return, onerror, goto, abort, throw, try, catch, fork, join, lock, unlock. NOT supported: continue.\n\n11. NO C/C++/JS IDIOMS: Do not use 'for (int i = 0; ...)' -- declare 'variable i;' at block top, then 'for (i = 0; ...)'."
          }
        ]
      }
    }
  }
}
